import React from 'react'

const QuoteSection = () => {
    // Ask user once if they would like to login for better experience.
    return (
        <div>QuoteSection</div>
    )
}

export default QuoteSection